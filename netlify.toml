[build]
  command = "echo \"const SUPABASE_URL = '$SUPABASE_URL'; const SUPABASE_ANON_KEY = '$SUPABASE_ANON_KEY'; const BUILD_TIME = '$(date +%Y-%m-%d\\ %H:%M\\ UTC)';\" > config.js"
  publish = "."
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
